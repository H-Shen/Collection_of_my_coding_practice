#include <iostream>

using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;
    if (a == b) cout << "C" << endl;
    if (c == b) cout << "A" << endl;
    if (a == c) cout << "B" << endl;
}