#include <bits/extc++.h>

using namespace std;
using namespace __gnu_pbds;

int main() {

    double a;
    int n;
    scanf("%lf %d", &a, &n);
    printf("%.3lf\n%d\n", a / n, n * 2);
    
    return 0;
}