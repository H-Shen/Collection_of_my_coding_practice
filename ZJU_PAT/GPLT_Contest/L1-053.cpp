#include <bits/stdc++.h>

using namespace std;

int main() {
    int a, b;
    cin >> a >> b;
    string s;
    for (int i = 0; i < a + b; ++i) {
        s += "Wang!";
    }
    cout << s << endl;
    return 0;
}