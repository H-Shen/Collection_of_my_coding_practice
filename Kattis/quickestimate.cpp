// https://open.kattis.com/problems/quickestimate
#include<bits/stdc++.h>

using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    int n;
    string s;
    cin >> n;
    while (n--) {
        cin >> s;
        cout << s.size() << endl;
    }
    return 0;
}
